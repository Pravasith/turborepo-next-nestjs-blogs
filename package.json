{
    "devDependencies": {
        "turbo": "^1.8.5"
    },
    "workspaces": [
        "apps/*"
    ],
    "private": true,
    "scripts": {
        "dev": "turbo run dev",
        "build": "turbo run build"
    }
}
