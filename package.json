{
    "devDependencies": {
        "turbo": "^1.8.5"
    },
    "workspaces": [
        "apps/*"
    ],
    "private": true,
    "scripts": {
        "dev": "turbo run dev",
        "start:server": "turbo run build --filter=server && turbo run start --filter=server",
        "start:client": "turbo run build --filter=client && turbo run start --filter=client"
    }
}
